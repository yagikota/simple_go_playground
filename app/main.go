package main

import (
	"fmt"
	"log"
	"net/http"
)



// func IndexHandler(w http.ResponseWriter, r *http.Request) {
// 	_, err := fmt.Fprint(w, "Hello, World!")
// 	if err != nil {
// 		log.Printf("indexHandler, %s", err.Error())
// 	}
// }

func main() {
	// mux := http.NewServeMux()
	// mux.HandleFunc("/", IndexHandler)
	// http.ListenAndServe(":8080", mux)
	
}
